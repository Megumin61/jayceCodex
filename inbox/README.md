# Inbox

这里用于暂存尚未判断项目归属的会话导出或工作记录。

归档时应完成以下动作：

1. 判断唯一主项目；
2. 移入 `projects/<project>/conversations/YYYY-MM-DD-<topic>/`；
3. 保留原始文件，补充 `README.md` 与 `INDEX.md`；
4. 更新项目上下文、交接页与根目录会话总索引。

不要把 Inbox 当作长期存储区。

